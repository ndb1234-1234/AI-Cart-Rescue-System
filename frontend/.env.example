# Base URL of the Flask/FastAPI backend serving the trained XGBoost model
VITE_API_BASE_URL=http://localhost:5000

# Set to "true" to force the frontend to always use mock predictions
# even if the backend is reachable. Useful for pure frontend demos.
VITE_FORCE_MOCK=false
